const bookings=pageStore.get('bookings',[]);const reference=new URLSearchParams(location.search).get('ref');const user=currentUser();const booking=bookings.find(item=>item.reference===reference&&(!user||item.passengerId===user.id));const ticketPage=page$('#ticket-page');if(booking){ticketPage.innerHTML=`<p class="eyebrow">DIGITAL TRANSPORT TICKET</p><div class="ticket-brand">ENTRACO <span style="float:right;color:var(--green);font-size:11px">✓ ${booking.status.toUpperCase()}</span></div><div class="ticket-route">${booking.from} → ${booking.to}</div><p class="muted">A confirmed intra-city journey across Enugu metropolis.</p><div class="ticket-grid"><span><small>PASSENGER</small><strong>${booking.passenger}</strong></span><span><small>BOOKING REFERENCE</small><strong>${booking.reference}</strong></span><span><small>DATE · DEPARTURE</small><strong>${booking.date} · ${booking.departure || 'N/A'}</strong></span><span><small>BUS NUMBER</small><strong>${booking.bus || 'N/A'}</strong></span><span><small>SEAT</small><strong>${booking.seat}</strong></span><span><small>FARE</small><strong>${pageMoney(booking.fare)}</strong></span></div><div class="demo-note" style="margin-top:22px">Use the booking reference when boarding.</div><button class="button print-button" onclick="window.print()">Print ticket <span>→</span></button>`}else ticketPage.innerHTML='<h1>No ticket found.</h1><p class="muted">A ticket can only be viewed by its passenger after payment.</p><a class="button" href="booking.html">Search trips <span>→</span></a>';
