const routeList=page$('#route-page-list');const routeData=pageStore.get('routes',[]);routeList.innerHTML=routeData.length?routeData.filter(route=>route.status!=='Inactive').map(route=>`<article class="route-card"><small>ACTIVE ROUTE</small><h3>${route.from} <span class="route-arrow">→</span> ${route.to}</h3><div class="route-bottom"><span><small>Fare</small><strong>${pageMoney(route.fare)}</strong></span><span><small>Estimated time</small><strong>${route.time || 'N/A'}</strong></span></div><a class="text-link" href="booking.html?from=${encodeURIComponent(route.from)}&to=${encodeURIComponent(route.to)}">View trip →</a></article>`).join(''):'<div class="panel"><strong>No routes available.</strong><p class="muted">An administrator must create an active route first.</p></div>';
